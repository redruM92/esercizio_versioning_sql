CREATE DATABASE testDB;

CREATE TABLE toscana (
    columnID int NOT NULL,
    columnA varchar(255) NOT NULL,
    columnB varchar(255),
    PRIMARY KEY (columnID)
);

CREATE TABLE lazio (
    columnID int NOT NULL,
    columnA varchar(255) NOT NULL,
    columnB varchar(255),
    PRIMARY KEY (columnID)
);